# Curator UI (Read-Only)

## Run locally

From the repository root:

```
geo-gsm-ui --input-dir <DIR>
```

Where `<DIR>` contains:

- `curation.jsonl`
- `evidence.jsonl`
- `suggestions.jsonl` (optional)

The UI is read-only and will not write any files.
